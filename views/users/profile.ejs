<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>

    <body>
        <header>
            <%- include("../partials/navbar") %>
        </header>
        <main>
            <div class="container">
                <div class="prueba">
                <h3 class="username text-center">
                    <%= user.username %>
                </h3>
                <div class="left-box-primary">
                    <img class="image" src="../img/avatar/<%= user.avatar %>" alt="" width="25px" height="25px" />
                    
                    <a href="#" class="btn btn-primary btn-block"><b>Editar foto</b></a>
                </div>
                </div>
                <div class="right tab-content">
                    <form class="form-horizontal">
                        <div>
                            <label for="inputName" class="col-sm-2 control-label">Name:</label>
                            <div>
                                <input type="text" id="inputName" placeholder="Nombre" value="<%= user.username%>">
                            </div>
                        </div>
                        <div>
                            <label for="inputEmail">Email</label>
                            <div>
                                <input type="email" id="inputEmail" placeholder="Email" value="<%= user.email %>">
                            </div>
                        </div>
                </div>
                <div>                    
                        <button type="submit" class="btn btn-danger">Edit User</button>                    
                </div>
                </form>
            </div>
            </div>
        </main>
        <footer>
            <%- include("../partials/footer") %>
        </footer>
    </body>

</html>