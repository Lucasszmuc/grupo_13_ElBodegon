<!-- <!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>

    <body>
        <header>
            <%- include("../partials/navbar") %>
        </header>
        <main>
            <div class="container">
                <div class="contenido-foto-edit">
                    <h2 class="username-text-center">
                        <% if (user && user.username) { %>
                            <p>Username: <%= user.username %>
                            </p>
                            <% } else { %>
                                <p>User data no disponible.</p>
                                <% } %>

                    </h2>
                    <div class="left-box-primary">
                        <img class="image" src="../img/avatar/<%= user.avatar %>" alt="" width="175px" height="175px" />
                    </div>
                </div>
                <div class="right tab-content">
                    <form class="form-horizontal" method="POST" action="/users/profile?_method=PUT"
                        enctype="multipart/form-data">
                        <div style="width: 40%;">
                            <label for="">Cambiar Avatar:</label>
                            <input type="file" name="avatar">
                        </div>
                        <div style="width: 20%;">
                            <label for="inputName" class="col-sm-2 control-label">Username:</label>
                            <div>
                                <input type="text" id="inputName" placeholder="Nombre" value="<%= user.username%>"
                                    name="username">
                            </div>
                        </div>
                        <div>
                            <label for="inputEmail">Email</label>
                            <div>
                                <input type="email" id="inputEmail" placeholder="Email" value="<%= user.email %>"
                                    name="email">
                            </div>
                        </div>
                        <input type="text" value="<%= user.id %>" name="id" style="display: none;">
                        <div class="boton-editar">
                            <button type="submit" class="btn btn-danger">Edit User</button>
                        </div>

                    </form>
                </div>
            </div>
        </main>
        <%- include("../partials/footer") %>
            <%- include('../partials/script') %>
    </body>

</html> -->

<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>

<body>
    <header>
        <%- include("../partials/navbar") %>
    </header>
    <main>
        <div class="container">
            <div class="profile-header">
                <% if (user && user.username) { %>
                    <h1>Username: <%= user.username %></h1>
                <% } else { %>
                    <h1>User data no disponible.</h1>
                <% } %>
            </div>
            <div class="profile-content">
                <div class="card avatar-card">
                    <img src="../img/avatar/<%= user.avatar %>" alt="User Avatar" class="avatar-image" />
                    <input type="file" name="avatar" class="avatar-upload">
                </div>
                <div class="card details-card">
                    <form method="POST" action="/users/profile?_method=PUT" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="inputName">Username:</label>
                            <input type="text" id="inputName" name="username" value="<%= user.username%>" class="form-control">
                        </div>
                        <div class="form-group">
                            <label for="inputEmail">Email:</label>
                            <input type="email" id="inputEmail" name="email" value="<%= user.email %>" class="form-control">
                        </div>
                        <input type="hidden" value="<%= user.id %>" name="id">
                        <button type="submit" class="btn btn-primary">Edit User</button>
                    </form>
                </div>
            </div>
        </div>
    </main>
    <%- include("../partials/footer") %>
    <%- include('../partials/script') %>
</body>

</html>
